using System;
using System.IO;

namespace MovieDatabase
{
    class Program
    {
        static void Main(string[] args)
        {
            // Local Instances
            UserInterface start = new UserInterface();

            // Start the program
            start.StartMenu();
        }
    }
}
